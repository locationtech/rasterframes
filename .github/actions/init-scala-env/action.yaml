name: setup scala
description: setup scala environment
runs:
  using: "composite"
  steps:
    - uses: coursier/cache-action@v7
    - uses: coursier/setup-action@v2
      with:
        jvm: zulu:8.0.362
        apps: sbt
