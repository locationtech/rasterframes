gdal {
  settings {
    options {
      // See https://trac.osgeo.org/gdal/wiki/ConfigOptions for options
      CPL_DEBUG = "ON"
    }
    // set this to `false` if CPL_DEBUG is `ON`
    useExceptions: false
  }
}